# 环境变量配置示例

# 数据库配置
DB_HOST=localhost
DB_PORT=5432
# DB_USER=postgres  # 可选，默认使用当前系统用户名（$USER 环境变量）
DB_PASSWORD=
DB_NAME=wayback
DB_SSLMODE=disable

# 服务器配置
SERVER_PORT=8080

# 数据存储目录
DATA_DIR=./data
